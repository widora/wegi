#include <stdio.h>
#include <stdlib.h>

#ifndef __FFUNC_H__
#define __FFUNC_H__

extern void  muzpage_update_title(const unsigned char *title);
extern void  muzpage_update_timingBar(int tm_elapsed, int tm_duration );

#endif
